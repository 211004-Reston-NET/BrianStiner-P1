@using System.Collections
@model ArrayList

<div class="container">
    <div class="row">
        <div class="col-md-12">
            @foreach (var item in Model)
            {<h3>@item</h3>}
        </div>
    </div>
    <div class="row">
